export default class Rol {
  constructor({ id_rol, nombre_rol, descripcion, estado }) {
    this.id_rol = id_rol
    this.nombre_rol = nombre_rol
    this.descripcion = descripcion
    this.estado = estado
  }
}
