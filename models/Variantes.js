class Variante {
  constructor({ id_variante, id_producto, id_talla, id_color, stock }) {
    this.id_variante = id_variante;
    this.id_producto = id_producto;
    this.id_talla = id_talla;
    this.id_color = id_color;
    this.stock = stock;
  }
}

export default Variante;
